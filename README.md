# 🚀 个人技术博客 / Personal Tech Blog

> 你的 `username.github.io` 个人名片站

## 📁 文件结构

```
blog/
├── index.html       # 主页面
├── style.css        # 样式
├── script.js        # 交互逻辑
├── posts.js         # 📌 文章列表（添加新文章改这里）
├── posts/           # 📝 文章内容（Markdown 文件）
│   ├── java-collections.md
│   └── mysql-index.md
└── README.md        # 你正在看的这个
```

## ✍️ 如何添加新文章

### 第 1 步：写 Markdown 文件

在 `posts/` 目录下创建一个 `.md` 文件，比如 `redis-cache.md`：

```markdown
# Redis 缓存三兄弟：穿透、击穿、雪崩

> 学习日期：2026-03-02 | 标签：Redis, 缓存

## 一句话总结
...（你的内容）
```

### 第 2 步：在 posts.js 中注册

打开 `posts.js`，在 `POSTS` 数组中添加一条：

```javascript
{
    id: "redis-cache",
    title: "Redis 缓存三兄弟：穿透、击穿、雪崩",
    date: "2026-03-02",
    tags: ["Redis", "缓存"],
    summary: "面试高频题：缓存穿透、击穿、雪崩的区别和解决方案。",
    file: "posts/redis-cache.md"
}
```

### 第 3 步：刷新页面

就这样，新文章自动出现在博客列表里！

---

## 🌐 部署到 GitHub Pages

### 第一次部署

1. **创建 GitHub 仓库**
   - 仓库名必须是 `你的用户名.github.io`（例如 `zhangsan.github.io`）
   - 设置为 **Public**

2. **把代码推上去**
   ```bash
   cd blog
   git init
   git add .
   git commit -m "🎉 init: personal tech blog"
   git remote add origin https://github.com/你的用户名/你的用户名.github.io.git
   git branch -M main
   git push -u origin main
   ```

3. **开启 GitHub Pages**
   - 进入仓库 → Settings → Pages
   - Source 选 `Deploy from a branch`
   - Branch 选 `main`，目录选 `/ (root)`
   - 点 Save

4. **等 1-2 分钟**，访问 `https://你的用户名.github.io` 🎉

### 后续更新

每次写了新文章，只需要：

```bash
git add .
git commit -m "📝 new post: redis缓存三兄弟"
git push
```

GitHub 自动帮你重新部署，1 分钟后新文章就上线了。

---

## 🎨 自定义

### 改名字和信息
在 `index.html` 中搜索 `J` 替换成你的名字，修改关于页里的个人信息。

### 改颜色
在 `style.css` 顶部的 `:root` 中修改颜色变量：

```css
--accent-1: #6c5ce7;     /* 主色调 - 紫色 */
--accent-2: #00cec9;     /* 辅色调 - 青色 */
--accent-3: #fd79a8;     /* 点缀色 - 粉色 */
```

### 加 GitHub 链接
在 `index.html` 中搜索 `https://github.com/` 替换成你的 GitHub 主页链接。
